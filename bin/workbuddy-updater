#!/usr/bin/env python3
"""开发入口：在 git checkout 内使用 ./bin/workbuddy-updater。"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))

from workbuddy_updater.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
